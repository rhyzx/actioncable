{
  "name": "actioncable",
  "version": "0.1.1",
  "description": "pre-built for Actioncable client",
  "main": "dist/actioncable.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bash build.sh",
    "prepublish": "npm run build && npm version --force $(bundle exec ruby version.rb)"
  },
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rhyzx/actioncable.git"
  }
}
