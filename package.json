{
  "name": "actioncable",
  "version": "5.0.0-beta3",
  "description": "pre-built for Actioncable client",
  "main": "dist/actioncable.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bash build.sh",
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags",
    "prepublish": "npm version $(bundle exec ruby version.rb)"
  },
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rhyzx/actioncable.git"
  }
}
